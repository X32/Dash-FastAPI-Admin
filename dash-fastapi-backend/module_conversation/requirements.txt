# 会话管理模块依赖
# 基础依赖（通常已在项目中存在）
sqlalchemy>=2.0.0
pydantic>=2.0.0
fastapi>=0.100.0

# 测试依赖
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0

# 可选依赖
# redis>=4.0.0  # 如果需要缓存支持
# aioredis>=2.0.0  # 异步Redis支持